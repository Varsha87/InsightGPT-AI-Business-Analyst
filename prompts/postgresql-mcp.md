# PostgreSQL MCP Configuration

The project uses the PostgreSQL MCP server in read-only mode to securely query the PostgreSQL database.

The MCP server enables:
- Table discovery
- Schema inspection
- SQL execution
- Safe read-only access
